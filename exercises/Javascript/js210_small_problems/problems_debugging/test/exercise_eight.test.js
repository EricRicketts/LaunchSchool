import { targetRoll } from "../code/glory";

describe('JS210 Small Problems Debugging Exercise Eight', function () {
  let myCharacter;
  beforeEach(() => {
    myCharacter = {
      name: 'Jenkins',
      strength: 4,
      constitution: 6,
      education: 11,
      luck: 3,
      sanity: 9,
    };
  });
  it('should ', function () {

  });
});